# keycloak-config-cli — generated from the plugin-factory corpus, then free to hand-edit.
# Selected for the post-rollback trait.
chart: keycloak-config-cli
repoURL: https://jkroepke.github.io/helm-charts/
version: 1.3.6

trait: post-rollback
traitObject: "Job/keycloak-config-cli"

notes: >-
  Artifact Hub rank 474. Installed bare, with no values
  overrides, so a failure here is the chart's prerequisites rather than
  formae's behaviour.
