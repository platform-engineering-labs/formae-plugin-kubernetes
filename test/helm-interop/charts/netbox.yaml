# netbox — generated from the plugin-factory corpus, then free to hand-edit.
# Selected for the test-hook trait.
chart: netbox
repoURL: https://charts.netbox.oss.netboxlabs.com/
version: 8.3.44

trait: test-hook
traitObject: "Pod/netbox-test-connection"

notes: >-
  Artifact Hub rank 167. Installed bare. A failure before
  formae is involved is the chart's prerequisites, not formae's behaviour,
  and is reported as a skip.
