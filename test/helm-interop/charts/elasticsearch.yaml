# elasticsearch — generated from the plugin-factory corpus, then free to hand-edit.
# Selected for the test-hook trait.
chart: elasticsearch
repoURL: https://helm.elastic.co
version: 7.17.3

trait: test-hook
traitObject: "Pod/elasticsearch-hluaa-test"

notes: >-
  Artifact Hub rank 25. Installed bare. A failure before
  formae is involved is the chart's prerequisites, not formae's behaviour,
  and is reported as a skip.
