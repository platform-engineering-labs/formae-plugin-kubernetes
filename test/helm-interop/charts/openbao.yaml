# openbao — generated from the plugin-factory corpus, then free to hand-edit.
# Selected for the test-hook trait.
chart: openbao
repoURL: https://openbao.github.io/openbao-helm
version: 0.28.5

trait: test-hook
traitObject: "Pod/openbao-server-test"

notes: >-
  Artifact Hub rank 433. Installed bare. A failure before
  formae is involved is the chart's prerequisites, not formae's behaviour,
  and is reported as a skip.
