# jenkins — generated from the plugin-factory corpus, then free to hand-edit.
# Selected for the test-hook trait.
chart: jenkins
repoURL: https://charts.jenkins.io/
version: 5.9.44

trait: test-hook
traitObject: "ConfigMap/jenkins-tests"

notes: >-
  Artifact Hub rank 15. Installed bare. A failure before
  formae is involved is the chart's prerequisites, not formae's behaviour,
  and is reported as a skip.
