# gitlab — generated from the plugin-factory corpus, then free to hand-edit.
# Selected for the test-hook trait.
chart: gitlab
repoURL: http://charts.gitlab.io/
version: 10.2.0

trait: test-hook
traitObject: "Pod/gitlab-webservice-test-runner-o97xa"

notes: >-
  Artifact Hub rank 12. Installed bare. A failure before
  formae is involved is the chart's prerequisites, not formae's behaviour,
  and is reported as a skip.
