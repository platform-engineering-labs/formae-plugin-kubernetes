# ingress-nginx — generated from the plugin-factory corpus, then free to hand-edit.
# Selected for the no-hooks trait.
chart: ingress-nginx
repoURL: https://kubernetes.github.io/ingress-nginx
version: 4.15.0

trait: no-hooks
traitObject: ""

notes: >-
  Artifact Hub rank 4. Installed bare, with no values
  overrides, so a failure here is the chart's prerequisites rather than
  formae's behaviour.
