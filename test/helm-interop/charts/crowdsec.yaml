# crowdsec — generated from the plugin-factory corpus, then free to hand-edit.
# Selected for the test-hook trait.
chart: crowdsec
repoURL: https://crowdsecurity.github.io/helm-charts
version: 0.23.0

trait: test-hook
traitObject: "Pod/crowdsec-test-agent"

notes: >-
  Artifact Hub rank 191. Installed bare. A failure before
  formae is involved is the chart's prerequisites, not formae's behaviour,
  and is reported as a skip.
