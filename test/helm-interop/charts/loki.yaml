# loki — generated from the plugin-factory corpus, then free to hand-edit.
# Selected for the test-hook trait.
chart: loki
repoURL: https://grafana.github.io/helm-charts
version: 7.1.0

trait: test-hook
traitObject: "Pod/loki-helm-test"

notes: >-
  Artifact Hub rank 9. Installed bare. A failure before
  formae is involved is the chart's prerequisites, not formae's behaviour,
  and is reported as a skip.
