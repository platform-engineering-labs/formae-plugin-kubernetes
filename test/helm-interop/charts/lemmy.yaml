# lemmy — generated from the plugin-factory corpus, then free to hand-edit.
# Selected for the test-hook trait.
chart: lemmy
repoURL: https://ananace.gitlab.io/charts
version: 0.6.14

trait: test-hook
traitObject: "Pod/lemmy-test-connection"

notes: >-
  Artifact Hub rank 363. Installed bare. A failure before
  formae is involved is the chart's prerequisites, not formae's behaviour,
  and is reported as a skip.
