# zammad — generated from the plugin-factory corpus, then free to hand-edit.
# Selected for the test-hook trait.
chart: zammad
repoURL: https://zammad.github.io/zammad-helm
version: 16.4.6

trait: test-hook
traitObject: "ConfigMap/zammad-helm-test-rake-task"

notes: >-
  Artifact Hub rank 192. Installed bare. A failure before
  formae is involved is the chart's prerequisites, not formae's behaviour,
  and is reported as a skip.
