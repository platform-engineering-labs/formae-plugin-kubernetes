# localstack — generated from the plugin-factory corpus, then free to hand-edit.
# Selected for the test-hook trait.
chart: localstack
repoURL: https://helm.localstack.cloud
version: 0.6.27

trait: test-hook
traitObject: "Pod/localstack-test-connection"

notes: >-
  Artifact Hub rank 410. Installed bare. A failure before
  formae is involved is the chart's prerequisites, not formae's behaviour,
  and is reported as a skip.
