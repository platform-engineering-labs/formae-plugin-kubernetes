# cluster — generated from the plugin-factory corpus, then free to hand-edit.
# Selected for the test-hook trait.
chart: cluster
repoURL: https://cloudnative-pg.io/charts/
version: 0.8.0

trait: test-hook
traitObject: "Job/cluster-ping-test"

notes: >-
  Artifact Hub rank 232. Installed bare. A failure before
  formae is involved is the chart's prerequisites, not formae's behaviour,
  and is reported as a skip.
