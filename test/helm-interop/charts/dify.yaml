# dify — generated from the plugin-factory corpus, then free to hand-edit.
# Selected for the test-hook trait.
chart: dify
repoURL: https://douban.github.io/charts/
version: 0.9.1

trait: test-hook
traitObject: "Pod/dify-test-connection"

notes: >-
  Artifact Hub rank 264. Installed bare. A failure before
  formae is involved is the chart's prerequisites, not formae's behaviour,
  and is reported as a skip.
