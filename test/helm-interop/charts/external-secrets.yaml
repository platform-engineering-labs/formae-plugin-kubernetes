# external-secrets — generated from the plugin-factory corpus, then free to hand-edit.
# Selected for the no-hooks trait.
chart: external-secrets
repoURL: https://charts.external-secrets.io/
version: 2.7.0

trait: no-hooks
traitObject: ""

notes: >-
  Artifact Hub rank 16. Installed bare, with no values
  overrides, so a failure here is the chart's prerequisites rather than
  formae's behaviour.
