# unleash — generated from the plugin-factory corpus, then free to hand-edit.
# Selected for the test-hook trait.
chart: unleash
repoURL: https://docs.getunleash.io/helm-charts/
version: 5.6.6

trait: test-hook
traitObject: "Pod/unleash-test-connection"

notes: >-
  Artifact Hub rank 306. Installed bare. A failure before
  formae is involved is the chart's prerequisites, not formae's behaviour,
  and is reported as a skip.
