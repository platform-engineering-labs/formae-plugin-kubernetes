# ambassador — generated from the plugin-factory corpus, then free to hand-edit.
# Selected for the crd-install trait.
chart: ambassador
repoURL: https://getambassador.io/
version: 6.9.4

trait: crd-install
traitObject: "CustomResourceDefinition/filters.getambassador.io"

notes: >-
  Artifact Hub rank 314. Installed bare, with no values
  overrides, so a failure here is the chart's prerequisites rather than
  formae's behaviour.
