# external-dns — generated from the plugin-factory corpus, then free to hand-edit.
# Selected for the no-hooks trait.
chart: external-dns
repoURL: https://kubernetes-sigs.github.io/external-dns/
version: 1.20.0

trait: no-hooks
traitObject: ""

notes: >-
  Artifact Hub rank 17. Installed bare, with no values
  overrides, so a failure here is the chart's prerequisites rather than
  formae's behaviour.
