/*
 * © 2025 Platform Engineering Labs Inc.
 *
 * SPDX-License-Identifier: Apache-2.0
 *
 * K8S Plugin Schema - Base definitions for Kubernetes resources
 * Generated by tools/gen-schema/generator.pkl
 */
module limitrange

import "@formae/formae.pkl"
import "../k8s.pkl"

const type = "K8S::Core::LimitRange"

open class LimitRangeResolvable extends formae.Resolvable {
  hidden type = module.type
  hidden name: LimitRangeResolvable = (this) { property = "metadata.name" }
}
@k8s.ResourceHint {
  type = module.type
  identifier = "$.metadata.name"
  discoverable = true
  parent = "K8S::Core::Namespace"
  listParam = new formae.ListProperty {
    parentProperty = "metadata.name"
    listParameter = "namespace"
  }
}
open class LimitRange extends k8s.Resource {
  hidden parent = this
  fixed hidden resourceType: String = module.type

  hidden res: LimitRangeResolvable = new {
    label = parent.label
    stack = parent.stack?.label
  }

  @k8s.FieldHint { required = true }
  apiVersion: String = "v1"

  @k8s.FieldHint { required = true }
  kind: String = "LimitRange"

  @k8s.FieldHint { required = true }
  metadata: k8s.NamespacedObjectMeta

  @k8s.FieldHint {}
  spec: LimitRangeSpec?
}

@k8s.SubResourceHint {}
open class LimitRangeSpec extends formae.SubResource {
  @k8s.FieldHint { required = true; updateMethod = "EntitySet"; indexField = "type" }
  limits: Listing<LimitRangeItem>
}

@k8s.SubResourceHint {}
open class LimitRangeItem extends formae.SubResource {
  @k8s.FieldHint { outputTransformation = (it) -> k8s.toK8sQuantityMapping(it) }
  default: k8s.ResourceQuantities?

  @k8s.FieldHint { outputTransformation = (it) -> k8s.toK8sQuantityMapping(it) }
  min: k8s.ResourceQuantities?

  @k8s.FieldHint { outputTransformation = (it) -> k8s.toK8sQuantityMapping(it) }
  max: k8s.ResourceQuantities?

  @k8s.FieldHint { outputTransformation = (it) -> k8s.toK8sQuantityMapping(it) }
  maxLimitRequestRatio: k8s.ResourceQuantities?

  @k8s.FieldHint { required = true }
  `type`: String

  @k8s.FieldHint { outputTransformation = (it) -> k8s.toK8sQuantityMapping(it) }
  defaultRequest: k8s.ResourceQuantities?
}
